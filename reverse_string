#include<stdio.h>
#include<string.h>
int main()
{
	char a[10],b[10];
	int c,i,j=0,l;
	printf("Enter string :-");
	scanf("%s",a);
	l=strlen(a)-1;
	for(i=l;i>=0;i--)
	{
		b[j]=a[i];
		j=j+1;
		
	}
	c=strcmp(a,b);
  
  printf("The reverse of the string is %s",b);
	return 0;
}